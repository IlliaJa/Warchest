[project]
name = "warchest"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "gymnasium~=1.1.1",
    "numpy<2",
    "matplotlib~=3.10.1",
    "torch",
    "wandb",
    "tensorboard",
    "tqdm",
]

[tool.uv]
extra-index-url = ["https://download.pytorch.org/whl/cu126"]
